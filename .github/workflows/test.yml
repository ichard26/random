name: testing-workflow

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # - uses: actions/setup-python@v2
      #   with:
      #     python-version: "3.6"

      - name: Create issue comment
        uses: peter-evans/create-or-update-comment@v1
        with:
          issue-number: 1
          body: $(cat .github/workflows/test.yml)
